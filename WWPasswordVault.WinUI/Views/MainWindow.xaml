<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="WWPasswordVault.WinUI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WWPasswordVault.WinUI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="WWPasswordVault.WinUI">
    
    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid x:Name="Root">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Grid Padding="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock
                    Grid.Column="0"
                    Text="User:"
                    VerticalAlignment="Center"
                    Margin="0 0 5 0"/>

                <TextBlock
                    Grid.Column="1"
                    Text="{Binding CurrentUser}"
                    VerticalAlignment="Center"/>
            </Grid>
            
            <CommandBar
                Grid.Column="1"
                HorizontalAlignment="Left"
                OverflowButtonVisibility="Collapsed"
                Visibility="{Binding CommandBarState}">
                
                <AppBarButton
                    Content="Logout"
                    Command="{Binding LogoutCommand}"/>
            </CommandBar>
        </Grid>

        <NavigationView x:Name="MainNav"
                        Grid.Row="1"
                        IsBackButtonVisible="Collapsed"
                        SelectionChanged="NavView_SelectionChanged"
                        PaneDisplayMode="Left"
                        HorizontalContentAlignment="Stretch">
            
            <NavigationView.MenuItems>
                <NavigationViewItem Icon="Home" Content="Home" Tag="home"/>
                <NavigationViewItem Icon="Add" Content="Add Vault Entry" Tag="editPage"/>
            </NavigationView.MenuItems>

            <Frame x:Name="ContentFrame"/>
        </NavigationView>
    </Grid>
</Window>
