<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="WWPasswordVault.WinUI.Views.EditPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WWPasswordVault.WinUI.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="10">
        <StackPanel Spacing="10">

            <TextBox
                Header="Program / Site"
                Text="{Binding Title, Mode=TwoWay}" />

            <TextBox
                Header="Username / E-Mail"
                Text="{Binding Username, Mode=TwoWay}" />

            <ComboBox
                Header="Category"
                ItemsSource="{Binding SavedCategorys}"
                SelectedItem="{Binding SelectedCategorys, Mode=TwoWay}"
                Text="{Binding Category, Mode=TwoWay}"
                IsEditable="True"
                HorizontalAlignment="Stretch"/>

            <PasswordBox
                Header="Password"
                Password="{Binding Password, Mode=TwoWay}" />

            <StackPanel Orientation="Horizontal" Spacing="8">
                <Button Content="Save" Command="{Binding SaveButton}" />
                <Button Content="Cancel" Command="{Binding CancelButton}" />
            </StackPanel>
            
            <TextBox
                Header="Category Tags"
                IsTabStop="False"
                Text="{Binding CategoryString}"
                IsReadOnly="True" />

        </StackPanel>

    </Grid>
</Page>
